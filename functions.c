#include <stdio.h>
#include <math.h>

/* void calculateSquare(int number)
{
   int square = number * number;
   printf("Square of %d is %d \n", number, square);
}
//-------------------------------------------------------------------------------------------------
void addNumbers(int num1, int num2)
{
   int sum = num1 + num2;
   printf("\nSum of %d and %d is %d", num1, num2, sum);
}
//-------------------------------------------------------------------------------------------------
int addNumber(int num1, int num2)
{
   int sum = num1 + num2;
   return sum;
}
//-------------------------------------------------------------------------------------------------
// prototype function is required if the fuctions is write after the main fuction
void productOfTwoNumbers(int num1,int num2);
void divisionOfTheNumber(int num1,int num2); */
int main()
{

   double result=sqrt(4);
   printf("Square root of 4 is %.1lf",result);
//-------------------------------------------------------------------------------------------------

/*    calculateSquare(5);
   addNumbers(3, 5);

   printf("\nSum of 3 and 5 is %d", addNumber(3, 5));
   productOfTwoNumbers(9,3);
   divisionOfTheNumber(9,3);
 */

   return 0;
}
/* //-------------------------------------------------------------------------------------------------
 void productOfTwoNumbers(int num1, int num2)
   {
      int product = num1 * num2;
      printf("\nProduct of %d and %d are %d", num1, num2, product);
   }
//-------------------------------------------------------------------------------------------------
void divisionOfTheNumber(int num1,int num2){
   double result=(double)num1/(double)num2;
   printf("\n%d divide by %d equal to %.2lf",num1,num2,result);
}
//------------------------------------------------------------------------------------------------- */
